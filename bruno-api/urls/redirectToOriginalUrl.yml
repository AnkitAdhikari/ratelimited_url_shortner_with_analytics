info:
  name: redirectToOriginalUrl
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/:alias
  params:
    - name: alias
      value: t
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
